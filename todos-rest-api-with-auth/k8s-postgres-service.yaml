apiVersion: v1
kind: Service
metadata:
    name: webook-postgresql
spec:
    selector:
        app: webook-postgresql
    ports:
        -   protocol: TCP
            # 我访问的端口
            port: 5432
            targetPort: 5432
    type: ClusterIP
